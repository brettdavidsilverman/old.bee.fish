TO DO

Fix javascript state saving to handle:
   Array properties "[]" : {}
   TypedArrays btoa
   Functions =>
   
Garbage collection
   Specifically over matrix arrays when resizing

Index:
   Parent -> Label
   id -> Label -> Value
   Label pointer -> Value -> Ids
   
Configurarion files
    cert.pem
    key.pem
    logfiles
    serving html from where
    pid files?